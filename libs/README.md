# HellasControl API Stub

Place `hellascontrol-2.0.0.jar` (or a newer compatible release) in this directory so Gradle can compile against the HellasControl API. The dependency is declared as `compileOnly`, so the jar is not bundled with the final mod artifact.
